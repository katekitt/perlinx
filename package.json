{
  "name": "perlinassets",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ katekitt/perlinassets",
    "create-local": "graph create --node http://localhost:8020/ katekitt/perlinassets",
    "remove-local": "graph remove --node http://localhost:8020/ katekitt/perlinassets",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 katekitt/perlinassets"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
